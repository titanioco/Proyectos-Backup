# Google OAuth Configuration Example
# IMPORTANT: This is a SAFE example file for repository commits
# 
# For a working application:
# 1. Copy this file to 'oauth.properties'
# 2. Replace the placeholder values with your actual Google OAuth credentials
# 3. Never commit the real oauth.properties file (it's in .gitignore)

# Your Google OAuth 2.0 Client ID (from Google Cloud Console)
# Replace with your actual Client ID
google.oauth.client.id=YOUR_GOOGLE_CLIENT_ID.apps.googleusercontent.com

# Client Secret (keep this secret!)
# Replace with your actual Client Secret
google.oauth.client.secret=YOUR_GOOGLE_CLIENT_SECRET

# OAuth redirect URI
# Update if using different port or domain
google.oauth.redirect.uri=http://127.0.0.1:8080/oauth2callback

# OAuth scopes
# These can remain as-is for most applications
google.oauth.scope=openid email profile

# OAuth URLs (these can stay as-is)
google.oauth.auth.url=https://accounts.google.com/o/oauth2/v2/auth
google.oauth.token.url=https://oauth2.googleapis.com/token
google.oauth.userinfo.url=https://www.googleapis.com/oauth2/v2/userinfo
